version: '3.8'

services:
  yuandi-erp:
    build: .
    container_name: yuandi-erp
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL}
      - NEXT_PUBLIC_SUPABASE_API_KEY=${NEXT_PUBLIC_SUPABASE_API_KEY}
      - SUPABASE_API_KEY=${SUPABASE_API_KEY}
      - SESSION_SECRET=${SESSION_SECRET}
      - NEXT_PUBLIC_APP_URL=http://your-nas-ip:3000
    volumes:
      - ./uploads:/app/uploads  # 파일 업로드용
    networks:
      - yuandi-network

  # 옵션: 로컬 PostgreSQL 사용 시 (Supabase 대신)
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: yuandi-postgres
  #   restart: unless-stopped
  #   environment:
  #     - POSTGRES_USER=yuandi
  #     - POSTGRES_PASSWORD=your-secure-password
  #     - POSTGRES_DB=yuandi_erp
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   networks:
  #     - yuandi-network

networks:
  yuandi-network:
    driver: bridge

volumes:
  postgres_data: